{
  "name": "@ponko2/botkit-script-loader",
  "description": "Botkit Script Loader",
  "version": "0.4.0",
  "author": "Takahito Nakano",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "botkit": "^0.0.14",
    "eslint": "^2.5.3",
    "eslint-config-ponko2": "^2.3.0"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "botkit"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "peerDependencies": {
    "botkit": "^0.0.14"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ponko2/botkit-script-loader.git"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps inline",
    "lint": "eslint .",
    "prepublish": "npm run --if-present build",
    "watch": "babel src --out-dir lib --watch --source-maps inline"
  }
}
